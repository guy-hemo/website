version: 1.0
openFiles:
  - README.md
  - app/main.py
tasks:
  - name: "Set up environment"
    description: "Installs dependencies and prepares the Flask app."
    command: |
      bash echo "Installing dependencies..."
      bash pip3 install -r requirements.txt
      bash echo "Setting up Flask app..."