version: 1.0

openFiles:
  - README.md
  - app/main.py

tasks:
  - name: "SSH Setup & Run Flask App"
    description: "Prepares SSH access and launches the Flask server"
    command: |
      echo "🔐 Preparing SSH access..."
      chmod +x setup.sh
      ./setup.sh
