version: 1.0
openFiles:
  - README.md
  - app/main.py
tasks:
  - name: "Set up environment"
    description: "Installs dependencies and prepares the Flask app."
    command: |
      echo "Installing dependencies..."
      pip3 install -r requirements.txt
      echo "Setting up Flask app..."