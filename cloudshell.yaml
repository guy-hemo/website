version: 1.0

openFiles:
  - README.md
  - app/main.py

tasks:
  - name: "Start Flask App"
    description: "Install dependencies and run the app"
    command: |
      chmod +x setup.sh
      ./setup.sh